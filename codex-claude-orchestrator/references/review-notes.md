# Review notes

The supplied documents contained plaintext SSH passwords and repeated them in prompts and copy commands. This skill removes them. Rotate any credential exposed in those documents and use SSH keys/agent, a secret manager, or environment injection outside tracked files. A skill is not a secret store.

The source workflows also mixed machine-specific paths, research policy, orchestration, PPT production, and generic coding. This skill treats paths, interpreters, proxy scripts, and profiles as runtime-checked fields; routes detailed server and agent procedures separately; and includes document/PPT work only when it supports the reproduction report.

The source's reverse-proxy language is tightened: record a proxy dependency and network mode, without claiming public egress for the remote host. Process launch must write and verify the PID alongside the current project's logs and outputs. Raw structured event output must be separated from human-readable diagnostics because mixed output is not necessarily valid JSONL.

Scientific safeguards retained and clarified: separate smoke, pilot, benchmark, and clinical/production claims; predeclare validation unit, threshold source, grouping, seed, metrics, stop rule, and baseline; preserve negative results; and change one variable at a time during diagnosis unless an explicit factorial design is recorded.
